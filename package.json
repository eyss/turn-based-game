{
  "name": "my-app-dev",
  "private": true,
  "workspaces": [
    "ui",
    "tests"
  ],
  "scripts": {
    "start": "npm run build:happ && cross-env HC_PORT=$(port) concurrently -k \"npm run start:happ\" \"npm run start -w ui\"",
    "test": "npm run build:happ && npm t -w tests",
    "start:happ": "hc sandbox clean && RUST_LOG=warn hc s generate ./example/workdir/example-elo.happ --run=$HC_PORT -a my-app network mdns",
    "package": "npm run build:happ && npm run package:ui && hc web-app pack workdir",
    "package:ui": "npm run build -w ui && cd ui/dist && bestzip ../dist.zip *",
    "build:happ": "npm run build:dnas && hc app pack ./example/workdir",
    "build:dnas": "npm run build:zomes && hc dna pack ./example/workdir",
    "build:zomes": "CARGO_TARGET_DIR=target cargo build --release --target wasm32-unknown-unknown"
  },
  "devDependencies": {
    "@holochain/conductor-api": "github:holochain/holochain-conductor-api#cc7738671551683baac4758ebeffac265bdc3348",
    "concurrently": "^6.2.1",
    "cross-env": "^7.0.3",
    "new-port-cli": "^1.0.0",
    "rimraf": "^3.0.2",
    "bestzip": "^2.2.0"
  },
  "engines": {
    "npm": ">=7.0.0"
  }
}
